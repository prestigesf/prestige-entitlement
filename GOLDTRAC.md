# GoldTrac

This repo does not mint. prestige-mcp mints.

```
cd ../prestige-mcp
npx tsx src/goldtrac-cli-mint.ts --app prestige-mcp --payload event.json --spine
```

Nine fields. ML-DSA-65. SOFTWARE. Same kid as every other app.
